<h1>Editing Course</h1>

<%= render 'form', course: @course %>

<%= link_to 'Show', school_course_path(school_id: @school.id, id: @course.id) %> |
<%= link_to 'Back', school_courses_path(school_id: @school.id) %>
